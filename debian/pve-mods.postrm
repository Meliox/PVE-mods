#!/bin/bash
set -e

case "$1" in
    remove)
        # Remove apt hook if it was installed via pve-mods-configure.
        rm -f /etc/apt/apt.conf.d/99-pve-mods
        ;;

    purge)
        rm -f /etc/apt/apt.conf.d/99-pve-mods
        rm -rf /etc/pve-mods
        rm -rf /var/lib/pve-mods
        ;;
esac

#DEBHELPER#
exit 0
